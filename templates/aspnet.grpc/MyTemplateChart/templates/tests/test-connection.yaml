apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "MyTemplateChart.fullname" . }}-test-connection"
  labels:
    {{- include "MyTemplateChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "MyTemplateChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
